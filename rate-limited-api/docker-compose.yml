version: '3.8'

services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      - RATELIMIT_MAX_REQUESTS_PER_MINUTE=5
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 3
