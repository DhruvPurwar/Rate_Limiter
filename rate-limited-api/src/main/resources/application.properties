server.port=8080

# Rate limiting config — override via env var RATELIMIT_MAX_REQUESTS_PER_MINUTE
ratelimit.max-requests-per-minute=5

# Actuator
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
